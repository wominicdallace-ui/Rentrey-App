<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Rentrey.Maui"
             x:Class="Rentrey.Maui.HomePage"
             BackgroundColor="#F5F5F5"
             Title="Home">

    <Grid RowDefinitions="Auto,*,70">
        <ScrollView Grid.Row="0" Grid.RowSpan="2">
            <VerticalStackLayout Spacing="10" Padding="10">
                <Grid ColumnDefinitions="Auto,*,Auto" Margin="0,15,0,0">
                    <Border Grid.Column="0"
                            HeightRequest="60"
                            WidthRequest="60"
                            Stroke="#FFD700"
                            BackgroundColor="#FFF8DC"
                            HorizontalOptions="Center"
                            VerticalOptions="Center"
                            Padding="0">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="50"/>
                        </Border.StrokeShape>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Tapped="LoadImage"/>
                        </Border.GestureRecognizers>
                        <Image Source="{Binding ProfileImageSource}"
                               HorizontalOptions="Center"
                               VerticalOptions="Center"
                               Aspect="AspectFill" />
                    </Border>

                    <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="5" Margin="10,0,0,0">
                        <Label Text="{Binding UserName}" FontSize="18" FontAttributes="Bold" TextColor="#333333"/>
                        <Label Text="{Binding Points}" FontSize="20" FontAttributes="Bold" TextColor="#4CAF50"/>
                        <BoxView HeightRequest="8" BackgroundColor="#D3D3D3" CornerRadius="4" HorizontalOptions="FillAndExpand">
                            <BoxView.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="#4CAF50" Offset="{Binding ProgressRatio}" />
                                    <GradientStop Color="#D3D3D3" Offset="{Binding ProgressRatio}" />
                                </LinearGradientBrush>
                            </BoxView.Background>
                        </BoxView>
                        <Label Text="{Binding LastUpdated}" FontSize="12" TextColor="#888888"/>
                    </VerticalStackLayout>
                </Grid>

                <Label Text="Recent Updates" FontSize="20" FontAttributes="Bold" TextColor="#333333" Margin="0,15,0,0"/>
                <CollectionView ItemsSource="{Binding RecentUpdates}" HeightRequest="200">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="20" BackgroundColor="White" Margin="0,5">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="10" />
                                </Border.StrokeShape>
                                <HorizontalStackLayout Spacing="10" VerticalOptions="Center">
                                    <Image Source="{Binding IconSource}" HeightRequest="40" WidthRequest="40" VerticalOptions="Center" />
                                    <VerticalStackLayout Spacing="2" VerticalOptions="Center">
                                        <Label Text="{Binding Title}" FontAttributes="Bold" TextColor="#333333" />
                                        <Label Text="{Binding Description}" TextColor="#888888" />
                                    </VerticalStackLayout>
                                </HorizontalStackLayout>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <Label Text="Newly Added Properties" FontSize="20" FontAttributes="Bold" TextColor="#333333" Margin="0,15,0,0"/>
                <CollectionView ItemsSource="{Binding NewlyAddedProperties}" HeightRequest="250">
                    <CollectionView.ItemsLayout>
                        <LinearItemsLayout Orientation="Horizontal" ItemSpacing="15"/>
                    </CollectionView.ItemsLayout>
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="0" WidthRequest="250">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="10" />
                                </Border.StrokeShape>
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type local:HomePage}}, Path=BindingContext.NavigateToPropertyCommand}"
                                                          CommandParameter="{Binding .}" />
                                </Border.GestureRecognizers>
                                <VerticalStackLayout>
                                    <Image Source="{Binding ImageSource}" Aspect="AspectFill" HeightRequest="180" />
                                    <VerticalStackLayout Padding="10">
                                        <Label Text="{Binding Details}" HorizontalOptions="Start" FontSize="14" FontAttributes="Bold"/>
                                        <Label Text="{Binding Address}" HorizontalOptions="Start" FontSize="16" FontAttributes="Bold" Margin="0,5,0,0" />
                                    </VerticalStackLayout>
                                </VerticalStackLayout>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>
