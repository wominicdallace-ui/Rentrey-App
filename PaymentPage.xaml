<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Rentrey.Maui"
             x:Class="Rentrey.Maui.PaymentPage"
             Title="Payments"
             BackgroundColor="#F5F5F5">

    <ScrollView>
        <VerticalStackLayout Spacing="20" Padding="15">

            <Frame CornerRadius="10" 
                   Padding="20" 
                   BackgroundColor="{StaticResource Primary}" 
                   Margin="0, 10, 0, 0">
                <VerticalStackLayout Spacing="10">
                    <Label Text="Current Balance Due"
                           FontSize="18"
                           TextColor="White"/>
                    <Label Text="{Binding CurrentBalance, StringFormat='{0:C}'}"
                           FontSize="36"
                           FontAttributes="Bold"
                           TextColor="White"/>
                    <Label Text="{Binding DueDate, StringFormat='Due: {0:MMM dd}'}"
                           FontSize="14"
                           TextColor="#FFD700"/>
                </VerticalStackLayout>
            </Frame>

            <Button Text="Make a Payment"
                    Command="{Binding MakePaymentCommand}"
                    BackgroundColor="#4CAF50"
                    TextColor="White"
                    CornerRadius="8"
                    FontSize="18"
                    Padding="15"/>

            <Label Text="Payment History"
                   FontSize="20"
                   FontAttributes="Bold"
                   TextColor="#333333"
                   Margin="0, 10, 0, 0"/>

            <CollectionView ItemsSource="{Binding PaymentHistory}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame CornerRadius="8" Padding="15" Margin="0, 5" BackgroundColor="White">
                            <Grid ColumnDefinitions="*, Auto">
                                <VerticalStackLayout Grid.Column="0">
                                    <Label Text="{Binding Description}" FontAttributes="Bold" TextColor="#333333"/>
                                    <Label Text="{Binding Date, StringFormat='{0:MMM dd, yyyy}'}" FontSize="12" TextColor="#888888"/>
                                </VerticalStackLayout>
                                <Label Grid.Column="1" 
                                       Text="{Binding Amount, StringFormat='{0:C}'}" 
                                       FontAttributes="Bold" 
                                       TextColor="{Binding StatusColor}"/>
                            </Grid>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>